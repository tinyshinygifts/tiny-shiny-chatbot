CHANGES_DONE

v43 update (base v42):

1. API Settings connection status now includes:
   - OpenAI / ChatGPT
   - Cloudinary Media

2. OpenAI status card:
   - Shows connected if OPENAI_API_KEY is saved.
   - Live test checks selected OPENAI_MODEL.
   - Shows useful error if key/model/billing/project access is wrong.

3. Cloudinary status card:
   - Shows connected if CLOUDINARY_URL is saved or Cloud Name + API Key + Secret are saved.
   - Shows Cloudinary cloud name in live log.
   - Shows error if Cloudinary is enabled but credentials are incomplete.

4. Status summary count includes OpenAI and Cloudinary.

5. Previous fixes preserved from v42:
   - Render/API keys fields in Chatbot API Settings
   - OpenAI/Cloudinary fields in API Settings
   - Submit to Meta button on every template card
   - WhatsApp inbound media/image support
   - CRM confirm/cancel/pending logic
   - Meta Ads AI product/category dropdown
   - Bulk Broadcast SKU/date product dropdown
   - Bandwidth Saver Mode

Base:
- v42 ZIP used as base.
