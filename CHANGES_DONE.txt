CHANGES_DONE

v25 update:
1. CRM Dashboard Shopify order visibility fixed.
   - Shopify order webhook leads are merged/backfilled into CRM when CRM loads.
   - CRM cards now show order number, amount, payment method, COD flag, WhatsApp status and order status.

2. order_confirmation exact Meta template format added.
   - Header Image + exactly 4 body variables + no button parameters.

3. COD customer Cancel reply -> Shopify cancel improved.
   - On customer Cancel/No/Nahi reply, Shopify cancel API is called with retry bodies.
   - Order tag/note update remains.
   - Customer/Admin messages now show whether Shopify cancel succeeded or failed.
   - CRM/lead status records Shopify cancel success/failure.

Base:
- v24 ZIP used as base.
