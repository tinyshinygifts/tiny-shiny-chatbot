CHANGES_DONE

v23 update:
1. Shopify order WhatsApp template mismatch fix.
   - Meta error #132012 now detected as template parameter mismatch.
   - Order confirmation/COD template sending now retries with:
     image header + variable counts,
     no-image/no-header + variable counts,
     template with no components when needed.
   - Dynamic URL button variables are now supported in order templates.
   - Normal order confirmation also retries without image if image/header format mismatch occurs.

2. CRM/debug error now shows clear reason:
   "Template variables mismatch. Please check Meta approved template body variables count, header type, and URL button variables."

Base:
- v22 ZIP used as base.
