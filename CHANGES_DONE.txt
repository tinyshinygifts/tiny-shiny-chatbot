CHANGES_DONE

v24 update:
1. COD order confirmation WhatsApp fixed according to Meta screenshot.
   Exact API format now sent for cod_order_confirmation:
   - Header: Image
   - Body: exactly 4 text variables
     {{1}} Customer Name
     {{2}} Product Name
     {{3}} Order Number
     {{4}} Order Amount
   - Buttons: Confirm / Cancel / Visit website
   - No button parameters are sent because these buttons are not dynamic URL placeholders.

2. This prevents Meta error #132012 caused by extra/mismatched button/header/body parameters.

3. Debug now stores:
   - exactMetaFormat: true
   - bodyVariableCount: 4
   - buttonParametersSent: false

Base:
- v23 ZIP used as base.
